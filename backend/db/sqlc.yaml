version: "2"
sql:
  - engine: "postgresql"  # ou "mysql"
    schema: "schema.sql"
    queries: "queries"
    gen:
      go:
        package: "db"
        out: "db/sqlc"
        sql_package: "database/sql"
